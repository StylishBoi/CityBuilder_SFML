#include "ui/hover.h"

/*namespace api::ui {
Hover::Hover() {
  texture_.loadFromFile("_assets/sprites/hover_sprite.png");
  hover_sprite_.setTexture(texture_);
  }

void Hover::Update(sf::Vector2f mousePos) {
  hover_sprite_.setPosition(mousePos);
}

void Hover::Draw(sf::RenderWindow& window) const {
  window.draw(hover_sprite_);
}
}*/